{
  "name": "study-planner",
  "version": "1.0.0",
  "description": "Full-stack Study Planner for Vercel deployment",
  "main": "server/server.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/server.js",
    "dev": "node server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "mongoose": "^7.0.0",
    "mysql2": "^3.23.2"
  }
}
